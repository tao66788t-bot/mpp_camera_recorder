# IPC 项目 Claim 实验记录模板（第一人称）

## 1. 本次实验目标

我这次要补证的 claim 是：

- `填写 claim 名称`

我希望通过这轮实验回答的问题是：

1. 当前工程里这项能力是否真的存在
2. 这项能力能不能被量化
3. 最终结果能不能安全写进简历

---

## 2. 实验前基线

### 2.1 使用版本

- 源码/二进制基线：
  - `填写版本名或备份名`
- 启动脚本：
  - `填写脚本`

### 2.2 现场硬件环境

- 板子：
  - `V853 / 100ask`
- Sensor：
  - `GC2053`
- LCD：
  - `480x800`
- 其他外设/样本输入：
  - `例如手机外放 / 色卡 / 拍手 / 蜂鸣器`

### 2.3 先看的证据

1. 文档：
   - `填写`
2. 当前代码：
   - `填写`
3. 已知样本 / 旧日志 / 备份版本：
   - `填写`

---

## 3. 实验假设

我当前的假设是：

- `填写假设`

我这样判断的依据是：

- `填写依据`

---

## 4. 实验步骤

### 4.1 部署步骤

我严格按项目硬规则执行：

1. `adb push`
2. `adb shell reboot`
3. `sleep 15`
4. `adb kill-server && adb start-server && sleep 2`
5. 再次 `adb push`
6. 单实例启动固件

### 4.2 样本准备

- 输入样本：
  - `填写`
- 样本文件路径：
  - `填写`

### 4.3 现场动作

1. `填写`
2. `填写`
3. `填写`

### 4.4 分析命令

```powershell
填写命令
```

---

## 5. 原始证据

### 5.1 运行日志关键片段

```text
填写日志
```

### 5.2 样本文件

- `填写 mp4 / wav / pcm / png / jpg / log`

### 5.3 分析脚本输出

```json
填写输出
```

---

## 6. 结果解读

### 6.1 我这次实际证明了什么

- `填写`

### 6.2 我还没有证明什么

- `填写`

### 6.3 这项 claim 现在属于哪一层

- `strong`
- `coarse`
- `experiment_ready`

保留最终结果：

- `填写最终判定`

---

## 7. 遇到的问题

### 7.1 问题现象

- `填写`

### 7.2 排查过程

1. `填写`
2. `填写`
3. `填写`

### 7.3 最终处理

- `填写`

---

## 8. 能不能写进简历

如果现在就改简历，我会这样处理：

- 可以直接写：
  - `填写`
- 只能讲方法，不能写死数字：
  - `填写`
- 当前先别写：
  - `填写`

---

## 9. 当前结论

这次实验之后，我对这项 claim 的结论是：

- `填写一句总括`

如果后面还要继续闭环，我下一步要补的是：

1. `填写`
2. `填写`
